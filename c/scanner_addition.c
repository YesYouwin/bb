#include <stdio.h>
#include <conio.h>

void main() {
    int x,y, total;

    printf("Enter The First Number: ");
    scanf("%d", &x);
    printf("Enter The Second Number: ");
    scanf("%d", &y);
    total = x + y;
    printf("sum: %d\n", total);
}